title: Event Gallery
fields:
  - handle: title
    field:
      type: text
      display: Section Title
      default: "Event Gallery"
  - handle: count
    field:
      type: integer
      display: Number of Events to Show
      default: 3
      instructions: First event appears as the large featured card. Remaining fill the smaller slots.
  - handle: show_all_link
    field:
      type: toggle
      display: Show "See all events" link?
      default: true
